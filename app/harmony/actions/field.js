export const setFIeldColor = (color) => ({type: 'SET_FIELD_COLOR', color});

export const setGridColor = (color) => ({type: 'SET_GRID_COLOR', color});

export const setFieldCtx = (ctx) => ({type: 'SET_FIELD_CTX', ctx});
