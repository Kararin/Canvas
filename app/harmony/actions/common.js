export const setSize = ({width, height}) => ({type: 'SET_SIZE', width, height});

export const setStep = (step) => ({type: 'SET_STEP', step});

export const setBegin = (begin) => ({type: 'SET_BEGIN', begin});