export const setAxisColor = (color) => ({type: 'SET_AXIS_COLOR', color});

export const setAxisCtx = (ctx) => ({type: 'SET_AXIS_CTX', ctx});

export const setExtremym = (points) => ({type: 'SET_EXTREMUM', points});
